linters-settings:
  govet:
    check-shadowing: true
  golint:
    min-confidence: 0
  gocyclo:
    min-complexity: 15
  maligned:
    suggest-new: true
  goconst:
    min-len: 3
    min-occurrences: 3
  misspell:
    locale: US
  lll:
    line-length: 120

linters:
  enable-all: true
  disable:
    - gochecknoglobals
    - dupl
    - unparam

run:
  skip-dirs:
    - vendor 

